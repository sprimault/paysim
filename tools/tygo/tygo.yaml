packages:
  - path: "github.com/sprimault/paysim/internal/api"
    output_path: "web/src/shared/model/api.ts"
    type_mappings:
      "time.Time": "string /* RFC 3339 */"
      "map[string]string": "Record<string, string>"
      "format.Amount": "number"
      "payzen.Customer": "Customer"
    frontmatter: |
      // Copyright 2026 Stéphane Primault <sprimault@users.noreply.github.com>
      // SPDX-License-Identifier: Apache-2.0
      //
      // Types générés depuis internal/api via `make web-types` (tygo).
      // Ne pas modifier à la main.

      import type { Customer } from './payzen';
  - path: "github.com/sprimault/paysim/internal/providers/payzen"
    output_path: "web/src/shared/model/payzen.ts"
    include_files: ["types.go"]
    type_mappings:
      "time.Time": "string /* RFC 3339 */"
      "map[string]string": "Record<string, string>"
      "format.Amount": "number"
      "json.RawMessage": "unknown"
      "domain.State": "string"
      "domain.Payment": "unknown"
    frontmatter: |
      // Copyright 2026 Stéphane Primault <sprimault@users.noreply.github.com>
      // SPDX-License-Identifier: Apache-2.0
      //
      // Types générés depuis internal/providers/payzen via `make web-types`.
      // Ne pas modifier à la main.
