# Kustomization racine — pointe vers la base, garde
# `kubectl apply -k deploy/k8s/` fonctionnel pour l'usage courant
# (mode mémoire).
#
# Pour activer la persistance SQLite :
#   kubectl apply -k deploy/k8s/overlays/sqlite/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base
